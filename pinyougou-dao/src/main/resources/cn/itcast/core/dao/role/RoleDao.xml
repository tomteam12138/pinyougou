<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.itcast.core.dao.role.RoleDao" >

    <select id="findOne" resultType="cn.itcast.core.pojo.role.Role" parameterType="int">
        select * from tb_role where id = #{id}
    </select>

    <select id="findRoleList" resultType="cn.itcast.core.pojo.role.Role">
        select * from tb_role
    </select>
</mapper>